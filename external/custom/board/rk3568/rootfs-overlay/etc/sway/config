# RK3568 Sway Configuration
# Minimal config for embedded system

set $term foot
set $mod Mod4

# Output configuration (HDMI)
output HDMI-A-1 resolution 1920x1080 position 0,0

# Basic keybindings
bindsym $mod+Return exec $term
bindsym $mod+d exec dmenu_run
bindsym $mod+Shift+q kill
bindsym $mod+Shift+c reload
bindsym $mod+Shift+e exec swaymsg exit

# Navigation
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4

# Layout
bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+space floating toggle

# Status bar with keybinding hints
bar {
    position top
    status_command while echo "Super+Enter:Terminal | Super+D:Menu | Super+Shift+Q:Close | Super+Shift+E:Exit | $(date +'%Y-%m-%d %H:%M:%S')"; do sleep 1; done
}
